ngDescribe({
    name: 'Test select-items component',
    modules: 'app',
    element: '<select-items></select-items>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
